var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/', function(req, res, next) {
 //renderizar la ventana del navegador con en routes/index.js o routes/users.js
});

module.exports = router;
